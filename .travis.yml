language: go
go:
  - "1.10.x"
sudo: true

before_install:
  - go get github.com/golang/mock/mockgen
           github.com/dominikznidar/govendor
           github.com/go-swagger/go-swagger/cmd/swagger
           golang.org/x/net/netutil
  - wget https://install.goreleaser.com/github.com/golangci/golangci-lint.sh && chmod +x golangci-lint.sh && sudo ./golangci-lint.sh -b /usr/local/bin v1.9.3
  - make

script:
  - make test
